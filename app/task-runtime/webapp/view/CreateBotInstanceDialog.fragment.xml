<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:f="sap.ui.layout.form"
>
    <Dialog
        title="Create Bot Instance"
        contentWidth="30rem"
    >
        <f:SimpleForm
            editable="true"
            layout="ResponsiveGridLayout"
            labelSpanXL="4"
            labelSpanL="4"
            labelSpanM="4"
            labelSpanS="12"
        >
            <f:content>
                <Label text="Task" />
                <Text text="{dialog>/taskName}" />

                <Label
                    text="Bot Type"
                    required="true"
                />
                <Select
                    selectedKey="{dialog>/botInstance/type_ID}"
                    items="{dialog>/botTypes}"
                >
                    <core:Item
                        key="{dialog>ID}"
                        text="{dialog>name}"
                    />
                </Select>

                <Label text="Sequence" />
                <StepInput
                    value="{dialog>/botInstance/sequence}"
                    min="0"
                    max="999"
                />
            </f:content>
        </f:SimpleForm>

        <buttons>
            <Button
                text="Cancel"
                press=".onCancelBotInstance"
            />
            <Button
                text="Save"
                type="Emphasized"
                press=".onSaveBotInstance"
            />
        </buttons>
    </Dialog>
</core:FragmentDefinition>
